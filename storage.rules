rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to all files in this bucket.
    // WARNING: This makes all storage content publicly readable and writable.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
